import'package:flutter/material.dart';
class FirstContext extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text(
        "First Content",
        style: new TextStyle(fontSize: 20.0),
      ),
    );

  }
}